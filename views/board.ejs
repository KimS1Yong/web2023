<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cheer Board</title>
  <style>
   body{
      font-family: Arial, sans-serif;
      background-image: linear-gradient(
      rgba(256,256,256,.8),
      rgba(256,256,256,.8)
      ), url("/image/board_back.jpg");
      background-repeat: no-repeat;
      background-size: 100% 400%;
      text-align: center;
      font-size: 20px;
   }
   #li{
      text-align: center;
   }
  </style>
</head>
<body>
  <h1>유럽 5대리그에 속한 한국선수들 응원 게시판</h1>
  
  <% if (cheers.length > 0) { %>
    <ul>
      <% cheers.forEach((cheer) => { %>
        <p><%= cheer.content %></p>
      <% }); %>
    </ul>
    <a href="/cheering">Posting form</a><br>
    <a href="/index0">Home</a>
  <% } else { %>
    <p>No cheers yet.</p>
  <% } %>
</body>
</html>
